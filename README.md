# Git and GitHub
## Git
![git](images/git.png)
## Git =>Git is a tool version control or software
- Version control system
- Keep track of changes
- Collabration on project
### Others
- Githlab
- Breanstalk
- PerForce
- Bitbucket

# Differnce between Git and Github
| Git | Github |
|---------|---------|
| tool / software | service|
| change control, version control | Host git repository/folder/source code |
| Provides CLI( Command line interface Tool)| Provides GUI Interface |
| Installed and maintained locally| maintained on cloud / web |

_underline text_

Normal text

```html
<h1>Test</h1>
```
# Git Command Line
```command
git config
git config --help
git add .
git restore .
git commit -m "message"
git push -u origin main
git log
git log --oneline
git diff
git reset --soft commit_id
git reset --hard commit_id
git status
git checkout commit_id
get checkout main
git checkout branch_name
```
